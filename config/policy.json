{
    "identity:list_authorization_codes": "rule:member_or_admin_or_owner",
    "identity:revoke_access_token": "rule:member_or_admin_or_owner",
    "identity:request_authorization_code": "rule:member_or_admin_or_owner",

    // The following policies are also required, but should already be in your 'policy.json',
    // since they are needed for OAuth1

    "identity:list_access_tokens": "rule:admin_required",
    "identity:get_access_token": "rule:admin_required",

    "identity:create_consumer": "rule:admin_required",
    "identity:get_consumer": "rule:admin_required",
    "identity:list_consumers": "rule:admin_required",
    "identity:delete_consumer": "rule:admin_required",
    "identity:update_consumer": "rule:admin_required",
}